package com.teamcitrus.factory_expansion.common.item;

import net.minecraft.world.item.Item;

public class CanisterItem extends Item {
    public CanisterItem(Properties properties) {
        super(properties);
    }
    //public CanisterItem(int uses, boolean canBreak) {
    //    super(new Item.Properties().stacksTo(1).component(FEComponents.CANISTER, new CanisterComponent(uses, canBreak)));
    //}
}
